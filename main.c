#include <stdio.h>
#include

#define MAX_SIZE 100 * 1024 * 1024

typedef struct {
    void* start;
    void* end;
} poolAlloc;

void poolAllocInit(poolAlloc* poolAlloc) {
    poolAlloc->start = malloc
}


int main() {
    printf("Hello, World!\n");
    return 0;
}
